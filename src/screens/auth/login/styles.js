import {
    StyleSheet
} from 'react-native';

export default StyleSheet.create({
    content: {
        alignItems: 'center',
        justifyContent: 'center',
        flex: 0
    },
    input: {
        paddingBottom: 15
    },
    button: {
        paddingTop: 40
    },
    header: {
        alignItems: 'flex-start',
        justifyContent: 'flex-end',
        padding: 35,
        flex: 0.5
    },
    title: {
        fontSize: 28,
        fontWeight: 'bold',
        color: '#485357'
    }
});
